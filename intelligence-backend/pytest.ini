[pytest]
# rootdir = intelligence-backend/, para que `app` sea importable sin instalar
# el paquete ni tocar PYTHONPATH a mano.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
